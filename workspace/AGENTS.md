# AGENTS.md — рабочая область OpenClaw

Эта папка — домашний каталог агента внутри контейнера (локально или в облаке).

## Первый запуск

- Идентичность и правила — в `IDENTITY.md`.
- При необходимости создайте `USER.md` с вашими предпочтениями.

## Модель

Локальный LLM обслуживает **vLLM** на `http://localhost:8000/v1`. Имя модели в конфиге — из переменной `SERVED_MODEL_NAME` (по умолчанию `local-coder`).

## Безопасность

- Не выносите наружу секреты и приватные данные.
- Не выполняйте разрушительные команды без явной просьбы пользователя.
- Длинные ответы лучше писать в файлы в этой рабочей области.
